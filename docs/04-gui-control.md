# Step 4 - Graphical Control

A graphical interface script is used to control the server.

This script uses:

- pkexec for administrative privileges
- Zenity for graphical dialogs

Example command:
```
pkexec /usr/local/bin/server-script
```
This allows the user to control the server using a graphical interface instead of the terminal.
